# 051826/051926 - Daily Work Log

Session started 051826 evening, continued through 051926.

## Decisions
- 1 omnibus PR (no phasing) for the 1.3.0 release per user choice
- Smart LAN-vs-cloud router for control commands (preferred over duplicate cloud entities)
- Webhooks startCharge/endCharge out of scope (V2C lacks signature spec)
- `local_update_interval` stored per ConfigEntry (not per-device) — current data model is one fallback_ip per entry
- Devcontainer image bumped to python:3.14 (aligned with CI matrix ceiling)
- Bandit `# nosec B104` markers required alongside ruff `# noqa: S104` — they are not equivalent
- `min_ha_version` belongs in `hacs.json`, NOT in `manifest.json` (hassfest rejects it)

## Meetings & Conversations
- User confirmed: 1 PR omnibus, cloud-duplicates as services-only with router, webhooks deferred, smoke-test with snapshot/restore, per-entry refresh interval, Python 3.14 in matrix while keeping HA compat
- User flagged bot review on PR (GitHub Advanced Security supply-chain warnings)

## Notes
- Comprehensive code review via 3 parallel Explore agents + Plan agent
- 32 cloud endpoints found implemented, 10 missing (now added)
- 8 of 14 documented Trydan writeable keys implemented (now full 14)
- 279 → 400 tests, 2 pre-existing failures (test_gather + test_init rate-limit) diagnosed and fixed
- Live smoke test against real Trydan XQUXDU @ 10.35.0.50 + cloud: 25/25 endpoints OK, 11/11 keys restored to pre-test snapshot
- PR #12 went through 3 push cycles: omnibus → CI gate fixes (bandit/pip-audit/hassfest) → pytest-asyncio compat → CodeQL SHA pinning
- Final state: 5/5 CI gates green on HEAD 839ae24

## End of Day Summary

**Key accomplishments**
- Released the 1.3.0 omnibus PR #12, bumping the integration from 1.1.6 (full API parity + smart routing + configurable LAN refresh + es.json + live smoke test + CI matrix 3.12/3.13/3.14 + SBOM + Dependabot + reusable security workflow)
- Live smoke test verified the actual Trydan endpoint
- Diagnosed and fixed every CI gate failure surfaced by the cycle (pytest CVE, pytest-asyncio↔pytest 9 incompat, hassfest schema, bandit nosec, CodeQL SHA pinning)
- Bot review (CodeQL supply-chain) fully resolved
- 7 knowledge nominations queued for auditor promotion

**Decisions made**
See "Decisions" above.

**Open items carried forward**
- Wait for user review on PR #12 → merge → tag-and-release v1.3.0 will auto-trigger
- Post-merge verification of v1.3.0 GitHub Release (SBOM files attached)
- Manual HACS reinstall smoke test in dev container (options flow + router behaviour)

**Tomorrow's priorities**
See `Task Board.md → Today`.

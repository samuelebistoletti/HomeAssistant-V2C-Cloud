# 091626 - Daily Work Log

## Decisions
- Clear a `cloud_auth_degraded` repair issue unconditionally on every successful cloud refresh cycle, not only when the in-memory `degraded` flag transitions True→False — a reload always starts that flag False, so the old gate could never clear an issue raised by a previous runtime.
- Merged PR #70 without a Codex review (it hit its usage quota) since all CI checks were green and the user explicitly asked to merge — user call, not a default I'd make unprompted.
- Always add a CHANGELOG.md entry with every version bump, proactively — per explicit user reminder, saved to long-term memory.
- User approved all 3 auditor SOP revisions from 090826 — promoted directly into `.claude/knowledge-base.md` (Dependency Management, Testing Gotchas, Project Patterns sections). Removed the 3 corresponding nominations plus one duplicate ([060926] pip-audit, never promoted, now superseded) from `.claude/knowledge-nominations.md`.
- User closed the unique_id-collision investigation (091126 point 3) permanently — decided it's "not relevant anymore", not worth picking back up.
- User asked to revert the Active Transport sensor's unique_id back to the `v2c_<device>_<key>` scheme — the beta.1 naming without the `v2c_` prefix was never requested and breaks the project's descriptive-id convention. Fixed at the source plus a registry migration for existing installs (not just a code rename) so upgraders don't lose entity_id/history/automations.
- The entry's update listener, not the options flow, now owns scheduling any reload — fixes the HA 2026.12.0 double-reload deprecation found earlier today.
- User asked to explain the `entity_id` prefix inconsistency (`trydan_`/`xquxdu_`/`garage_xquxdu_`) on the live device; after explaining it's a normal HA one-time-generation artifact (not a bug, not something the integration controls), user said explicitly not to touch it.
- User asked to promote 1.4.0 to stable directly, without waiting for a separate live check of beta.11's two specific changes — did it on explicit instruction, noted the gap in Open Threads rather than silently assuming it was covered.

## Meetings & Conversations
- User reported (screenshot) that the "V2C Cloud non autentica" repair card kept showing even after V2C fixed cloud reachability and the user regenerated + re-entered a new API key.

## Notes
- Verified live via HA MCP: `binary_sensor.trydan_stato_connessione_v2c_cloud` was available (cloud healthy) while the `cloud_auth_degraded_...` repair issue was still `active: true` in the registry — confirmed the orphaned-issue theory empirically before touching code.
- GitHub issue #54 thread shows V2C's own support (case V2C-36367, comment from @koiottech 091526) confirms the underlying Cloud auth incident is STILL ACTIVE on their side as of 15/09, despite an earlier "service is stable" message. Our fix (issue no longer sticks) is independent of whether V2C's outage itself is resolved.
- HA core 2026.9.2 deprecation warning about `v2c_cloud`'s update-listener + manual `async_reload()` pattern in `V2COptionsFlow._apply` (breaks in HA 2026.12.0) — found this morning, fixed this afternoon in PR #71.
- When the user asked "sei sicuro che il punto 1 sia ok per tutte le entità?", did a full re-verification (grepped every `_attr_unique_id` site across all 6 platform files, checked for `def unique_id` property overrides, counted entity classes per file, checked every `unique_suffix` string) before answering — worth doing systematically rather than trusting the first targeted find.
- Fixed the 2 UI bugs deferred on 091126 (`step_id="user"` vs `"cloud"`; missing `cannot_connect_local` translation) in the same PR, per user request.
- Also reworded the `cloud_auth_degraded` repair message to drop the September-2026-incident reference it still had — direct violation of the 091126 "never cite the incident outside the GitHub issue" rule, only caught now while investigating the same message for the reload bug.

## End of Day Summary
- PR #70 opened, CI went green, Codex review unavailable (usage quota), merged (squash) on user's request. Dependabot PR #69 (ruff bump) merged in the same pass. **1.4.0-beta.10 released.**
- Issue #54: 2 comments posted — (1) root-cause + PR #70 link; (2) correction after V2C confirmed at 17:40 the underlying API instability is fixed on their side (superseding the "still active per case V2C-36367" status from comment 1) — told affected users to just regenerate their API key.
- User confirmed beta.10 live verification on XQUXDU was OK (manual IP removal, negative contracted power, repair auto-clear, new API key).
- 3 auditor SOP revisions from 090826 approved by user and promoted into `knowledge-base.md`; unique_id-collision investigation (091126 point 3) closed permanently at user's request.
- PR #71 opened for the unique_id-scheme revert + HA 2026.12.0 deprecation fix, CI green, Codex again out of quota, merged on user's request. **1.4.0-beta.11 released.**
- Explained the `entity_id` prefix inconsistency on XQUXDU via MCP (HA one-time-generation quirk, not a bug); user said leave it alone.
- **Promoted 1.4.0 to stable** on direct instruction: manifest bump straight to `main` (no PR, matching the 1.3.0 precedent), CHANGELOG.md aggregated into a single `[1.4.0]` entry synthesizing all 11 betas (each collapsed to a one-line "Folded into [1.4.0]" pointer), README test-count refreshed in both copies. Tag `v1.4.0` published as a non-prerelease.
- Cleaned up all merged/stale branches after every merge, remote and local; only `main` remains.
- Caught my own local `main` checkout was stale after merging via the GitHub API directly (never `git pull`ed) — synced before reporting anything as done, more than once today.
- 1.4.0 stable ships with beta.11's two changes (unique_id migration, reload-on-mode-change) never separately live-verified — flagged as an open thread rather than assumed covered.

# artifacts
__pycache__
.pytest*
*.egg-info
*/build/*
*/dist/*
*.zip

# CI generated files
bandit-report.json
release_body.md

# misc
.coverage
.vscode
coverage.xml
.ruff_cache
.venv
.env
.env.dev
# legacy per-token files (superseded by .env.dev) — still ignored if present
.gh-token
.v2c-token
.hass-token

# macOS Finder metadata
.DS_Store

# Claude Code
.claude

# Home Assistant configuration — local dev HA state, never committed
config/


# Claudify — personal & runtime files
CLAUDE.local.md
.claude/logs/
.claude/agent-memory/
.claude/backups/
.claude/hooks/__counter
__NEEDS_ONBOARD

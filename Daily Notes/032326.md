# 032326 - Daily Work Log

## Decisions
- Removed retry-on-429 entirely: retrying an exhausted rate limit burns quota 3× faster and prevents recovery. Raise immediately, let coordinator backoff.
- Coordinator exponential backoff on rate limit: 120s → 240s → 480s → 600s (cap 10 min), auto-restore on first success.
- Proactive pacing via `RateLimit-Remaining`: when < 150 remaining, stretch interval to pace budget over 24h worst-case, reserving 50 calls for commands.

## Meetings & Conversations
- User shared live HA log showing 6+ hours of consecutive 429s on v2c_cloud — diagnosed as self-reinforcing rate-limit loop.
- User confirmed V2C Cloud API official limit is 1000 calls/day and that polling consumes the vast majority; commands are minimal.

## Notes
- V2C Cloud rate limit: 1000/day. Successful responses include RateLimit-Limit and RateLimit-Remaining headers. No reset timestamp in headers.
- The 3-retry loop on 429 was the root cause: 3 × 720 cycles/day = 2160 attempted calls, burning the quota the moment it reset.
- Normal operation (pairings cached): ~750 calls/day — within budget with room for commands.
- The self-loop trigger: pairings never cache when always rate-limited → every cycle is 3 pairings calls → budget exhausted immediately after reset → infinite loop.

## End of Day Summary
- Diagnosed and fixed a persistent rate-limit loop from a user's live HA log
- Three fixes applied: no retry on 429 (Fix 1), coordinator exponential backoff (Fix 2), proactive RateLimit-Remaining pacing (Fix 3)
- Tests updated (352 total, all green): removed obsolete 3×429 mock patterns, added backoff and pacing tests
- Release 1.1.5 prepared: CHANGELOG + manifest bumped, pending commit/push
- Tomorrow: commit and push 1.1.5

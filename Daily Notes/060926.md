# 060926 - Daily Work Log

## Decisions
- Promote `1.3.0-beta.3` → `1.3.0` stable (user: beta window closed, no regressions). No code changes vs beta.3.
- CHANGELOG consolidated: single stable `[1.3.0] - 2026-06-09` folds in the old 1.3.0 candidate notes + beta.2/beta.3 detail; beta.1/2/3 collapsed to dated pointer entries. Removes the duplicate `[1.3.0]` header that would have collided.
- pip-audit fix: ignore the 2 CVEs on the **test-deps** step only (aiohttp is test-only — manifest `requirements: []`, HA core provides it at runtime). Runtime audit stays --strict, zero ignores.

## Meetings & Conversations
- User flagged 2 daily GitHub Actions failure emails: Security/pip-audit + Close inactive issues.

## Notes
- **Root causes found (via GH API logs, not guessed):**
  - pip-audit: aiohttp 3.13.5 pin → CVE-2026-34993 + CVE-2026-47265 (fixed in 3.14.0). Can't bump: aioresponses 0.7.8 (latest) ABI-breaks on aiohttp 3.14.x (verified 3.14.1 still breaks — missing `stream_writer`).
  - close-issues: `dessant/lock-threads@v6.0.0` error `"github-token" length must be <= 100 characters` — GITHUB_TOKEN outgrew the Joi cap. Fixed in v6.0.2 (#55, cap→1000). `actions/stale` was succeeding but on deprecated Node 20 → bumped to v10.3.0 (Node 24) ahead of 2026-06-16 enforcement.
- Verified green: 474 tests, ruff lint+format, both pip-audit steps, READMEs identical, manifest=1.3.0.
- 2 commits on main, NOT pushed/tagged: 8f0da6c (ci fixes), c311e27 (release 1.3.0).
- **Open before release:** push + tag `v1.3.0` (fires tag-and-release.yaml). Awaiting user go-ahead.
- Tag history gap: only `v1.3.0-beta.1` is tagged; beta.2/beta.3 were never tagged → no SBOM artifacts exist for them. SBOM-verify task only applies to beta.1 + the upcoming v1.3.0.

## End of Day Summary
- **Accomplishments:**
  - Shipped **v1.3.0 stable** (promoted from beta.3) — release + SBOMs published, 9/9 gates green. Consolidated CHANGELOG, both READMEs identical.
  - Fixed both daily-failing CI workflows (pip-audit test-only CVEs + close-issues lock-threads token cap / stale Node 20) — verified green via live runs. The pip-audit fix was the linchpin that had been silently blocking every release since beta.3.
  - Handled the 2 open Dependabot PRs (#26 ruff, #29 action-gh-release v3) and shipped **v1.3.1** maintenance release incorporating both. Corrected Dependabot's stale `# v2` comment.
- **Decisions:** see top of note. Plus: v3.0.0 action-gh-release vetted as Node-24-runtime-only (no API break) before merge; cut 1.3.1 patch for dep/CI-tooling only (no functional change).
- **Carried forward:** user HA-UI cloud-only (4G) manual test of routed writers (only item I can't do). Decide HACS beta-channel future now that stable is out.
- **Tomorrow's priorities:** (1) await/triage any v1.3.0/v1.3.1 community feedback; (2) optionally tackle a Backlog refactor (service dispatcher → data-driven ServiceSpec, or split `_async_update_data`) if no incoming issues.
- **Releases today:** v1.3.0, v1.3.1 (both stable, both with SPDX+CycloneDX SBOMs).

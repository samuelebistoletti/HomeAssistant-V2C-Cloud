# 051926 - Daily Work Log

Tuesday, 2026-05-19. Continuation of the 1.3.0 release cycle (omnibus PR #12 from 051826).

## Decisions
- Release `1.3.0` as `1.3.0-beta.1` pre-release first (HACS pre-release channel) rather than jumping to stable — early adopters exercise the omnibus before promotion.
- `tag-and-release.yaml` auto-detects prereleases from tag suffix (no extra workflow needed) and passes `prerelease: true` to the GitHub Release step.
- Accepted all 12 outstanding Dependabot PRs in one sweep — each gates green individually, batching cuts churn.

## Meetings & Conversations
- User approved beta-first release strategy and the security.yaml concurrency fix.
- User flagged the security.yaml concurrency conflict surfacing during the tag push (PR + tag workflows racing the same group key).

## Notes
- PR #12 merged at dddc1d9. No conflicts, all 5 gates green on HEAD 839ae24 going in.
- Dependabot wave merged in PR order #13 → #24: aioresponses, voluptuous, pip, hassfest, codecov, upload-artifact, setup-python, lock-threads, ruff+colorlog, pytest-asyncio, aiohttp, pyyaml. Working tree clean afterwards.
- Bumped manifest to `1.3.0-beta.1`, pushed tag `v1.3.0-beta.1`, tag-and-release ran and produced a prerelease (auto-detected from tag suffix).
- 3 new knowledge nominations queued for auditor: beta-channel release pattern, security workflow concurrency conflict, exit-code-3 pytest diagnosis (carried from earlier today).

## End of Day Summary

**Key accomplishments**
- PR #12 (omnibus 1.3.0) merged into main.
- `v1.3.0-beta.1` cut and published on HACS pre-release channel.
- 12 Dependabot PRs accepted (deps fully bumped, all CI gates green).
- CI hardened: security.yaml concurrency conflict resolved, tag-and-release auto-detects prereleases.
- Memory + Task Board + knowledge nominations synced.

**Decisions made**
See "Decisions" above.

**Open items carried forward**
- Monitor `v1.3.0-beta.1` for community feedback / regression reports.
- Verify GitHub Release `v1.3.0-beta.1` has SBOM artifacts (SPDX + CycloneDX) attached.
- Promote `1.3.0-beta.1` → `1.3.0` stable when the beta proves clean.
- Manual HACS reinstall smoke test in dev container (options flow + router).

**Tomorrow's priorities**
See `Task Board.md → Today`.

---

## Evening continuation (~19:30 → 20:25)

User flagged i18n + cloud-only entity bugs on the live install. Pivoted to a UX-fix burst on `main`.

### Decisions
- `connection_type` toggle made editable in the options flow — switching modes triggers `async_create_task(async_reload)`. In cloud-only the sentinel `fallback_ip=""` wins even if the user types a LAN IP in the same form.
- Phase B split into two: B1 (mapping extensions, harmless) and B2 (availability gating, slightly opinionated). Both shipped.
- For the device data needed to validate cloud-only mappings, dumped the real `/device/reported` + `/device/currentstatecharge` from the user's Trydan (XQUXDU, firmware 2.4.6) via `gh`-style direct curl with the user-provided V2C Cloud API token.

### Notes
- 6 commits to main, all CI green: 7589b23 (i18n), e01163d (toggle), 187e2bb (ruff format follow-up), fcbc404 (.gh-token), 93b91da (B1), b14a491 (B2). Tests 400 → 446.
- Cleaner mental model: `_REPORTED_TO_REALTIME` synthesises numeric fields; the new `_REPORTED_STRING_FIELDS` map handles string passthrough (the original loop silently dropped strings via `float(str(raw))`); `LAN_ONLY_KEYS` is the source of truth for "structurally absent in cloud" entities, with `V2CEntryRuntimeData.cloud_only` as the per-entry flag the entities consult in `available`.
- Real cloud key surprise: `contract_power` (singular, underscore) — *not* `contractedpower` as the pre-existing alias assumed. That mismatch was why the slider always read empty.
- gh CLI gotcha: `gh auth login --with-token` enforces a `read:org` scope check that fine-grained / classic PATs without org membership reject. Workaround: `GH_TOKEN=...` env var skips the validation and authenticates anyway.

### End-of-evening status
- Working tree clean, main at `b14a491`. 
- Next concrete action: user reload-test in HA to confirm recovered entities populate and LAN-only ones flip to "Non disponibile". If clean → tag `1.3.0-beta.2` or roll straight to `1.3.0` stable.
